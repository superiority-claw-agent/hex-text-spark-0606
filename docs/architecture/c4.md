# C4 Architecture -- hex-text-spark-0606

## Level 1: System Context

```mermaid
graph TD
    User["fa:fa-user Developer / technical user"]
    System["Hex Text Spark<br/>Static browser tool for UTF-8 text and hex conversion"]
    Browser["Web Platform APIs<br/>TextEncoder, TextDecoder, Clipboard API"]
    Host["Static Hosting<br/>GitHub Pages / Netlify / similar"]

    User -->|"Type, paste, convert, copy"| System
    System -->|"Uses local browser capabilities"| Browser
    User -->|"Loads app over HTTPS"| Host
    Host -->|"Serves static assets"| System
```

## Level 2: Container Diagram

```mermaid
graph TD
    subgraph "Hex Text Spark"
        UI["Static Web UI<br/>HTML + CSS + TypeScript<br/>Renders panels, actions, and responsive layouts"]
        Engine["Conversion Engine<br/>Pure TypeScript modules<br/>UTF-8 encode/decode, validation, formatting"]
        Tests["Local Test Suite<br/>Vitest or similar<br/>Validates conversion and formatting behavior"]
    end

    User["fa:fa-user User"] -->|"HTTPS"| UI
    UI -->|"Function calls"| Engine
    Tests -->|"Unit and integration checks"| Engine
    UI -->|"Invokes"| BrowserAPI["Browser APIs<br/>TextEncoder/TextDecoder/Clipboard"]
```

## Level 3: Component Diagram

```mermaid
graph TD
    subgraph "Static Web UI"
        AppShell["App Shell"]
        InputPanels["Text + Hex Panels"]
        ActionBar["Direction + Quick Actions"]
        FormatControls["Formatting Controls"]
        StatusRegion["Status / Error Region"]
        ExamplePresets["Example Presets"]
    end

    subgraph "Conversion Engine"
        Normalizer["Hex Normalizer"]
        Parser["Hex Parser"]
        UtfCodec["UTF-8 Codec"]
        Formatter["Hex Formatter"]
        Validators["Input Validators"]
    end

    AppShell --> InputPanels
    AppShell --> ActionBar
    AppShell --> FormatControls
    AppShell --> StatusRegion
    AppShell --> ExamplePresets

    InputPanels --> Validators
    ActionBar --> Parser
    ActionBar --> UtfCodec
    FormatControls --> Formatter
    ExamplePresets --> InputPanels
    Validators --> Normalizer
    Parser --> Normalizer
    Parser --> UtfCodec
    UtfCodec --> Formatter
    Parser --> StatusRegion
    UtfCodec --> StatusRegion
```

## Integration Boundaries

- No application backend is required for the initial release.
- Conversion logic runs entirely inside the browser runtime.
- Clipboard access is the only user-agent integration beyond rendering and encoding APIs.
- Static hosting is a deployment concern only; it does not participate in conversion behavior.

## Runtime Flow

```mermaid
sequenceDiagram
    participant U as User
    participant UI as UI Layer
    participant V as Validators/Normalizer
    participant C as UTF-8 Codec
    participant F as Formatter

    U->>UI: Paste text or hex
    UI->>V: Normalize and validate input
    alt text to hex
        UI->>C: Encode text to bytes
        C->>F: Format bytes as hex
        F-->>UI: Rendered hex
    else hex to text
        V->>C: Decode bytes with fatal UTF-8 mode
        C-->>UI: Decoded text or decode error
    end
    UI-->>U: Updated output and status
```

## Architectural Notes

- The conversion engine should remain framework-agnostic so it can be tested without DOM coupling.
- Parsing, decode, and formatting concerns should stay separate to keep validation failures precise.
- The UI state model should preserve raw input and rendered output independently to avoid destructive formatting side effects.
